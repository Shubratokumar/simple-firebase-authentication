/***************************************************************************
* Steps to use firebase
* 1. Create a project on console.firebase.google.com ;
* 2. install firebase ;
* 3. Register Web app in firebase ;
* 4. Copy firebase init with config from firebase project setting into a file firebase.init.js;
* 5. Export default app;
* 6. Import app from firebase into app.js;
* 7. Create auth variable
        Initialize Firebase Authentication and get a reference to the service; 
        const auth = getAuth(app);
* 8. Import getAuth from firebase/auth.
* 9. Turn on google authentication.
* 10. Click on Google and enable google signin method. Give a meaningful project name. Set project support email address. Then save the project. 
* 11. Crate GoogleAuthProvider;
* 12. Sign in with Pop-up;
* 13.

********************************************************************************/